// Default runtime config. Ships empty; local dev uses the Firebase Emulator
// (environment.ts) and ignores this. The production build overwrites this file
// in dist with public values via tools/build/generate-env-js.mjs. No secrets.
window.__env = {};
