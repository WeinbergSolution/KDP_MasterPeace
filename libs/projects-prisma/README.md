# projects-prisma

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build projects-prisma` to build the library.

## Running unit tests

Run `nx test projects-prisma` to execute the unit tests via [Vitest](https://vitest.dev/).
