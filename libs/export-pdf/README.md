# export-pdf

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build export-pdf` to build the library.

## Running unit tests

Run `nx test export-pdf` to execute the unit tests via [Vitest](https://vitest.dev/).
