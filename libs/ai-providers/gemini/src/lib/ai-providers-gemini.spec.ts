import { aiProvidersGemini } from './ai-providers-gemini.js';

describe('aiProvidersGemini', () => {
  it('should work', () => {
    expect(aiProvidersGemini()).toEqual('ai-providers-gemini');
  });
});
