# export-epub

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build export-epub` to build the library.

## Running unit tests

Run `nx test export-epub` to execute the unit tests via [Vitest](https://vitest.dev/).
