# export-docx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build export-docx` to build the library.

## Running unit tests

Run `nx test export-docx` to execute the unit tests via [Vitest](https://vitest.dev/).
